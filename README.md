## 
Initially we have set up a screening step using Bloomberg's OpenFIGI (Financial Instrument Global Identifier).

### Setup
Download the latest quarterly scores from [Inspire Insight's](https://inspireinsight.com/score-updates), and place them in the `/data` directory.
```bash
python -m venv env
pip install -r requirements.txt
python main.py
```

##